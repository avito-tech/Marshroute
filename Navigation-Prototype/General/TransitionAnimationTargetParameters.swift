import UIKit

/**
 *  Параметры анимации перехода, получаемые из информации о конечной точке
 *  Описывает параметры, передаваемые в сообщения вида push, pop
 */
struct TransitionAnimationTargetParameters {
    let viewController: UIViewController
}